# Copy this file to `.env` and fill with YOUR new, separate Supabase project values.
# Do NOT commit the real .env. These are placeholders only — no real credentials here.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
